import React from "react"

const Section = ({children}) => {
    return (
        <section
            className=""
        >
            <div>
                {children}
            </div>
        </section>
    )
}

export default Section
